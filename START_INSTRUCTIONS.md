# 🚀 ИНСТРУКЦИЯ ПО ЗАПУСКУ ОБНОВЛЕННОГО БОТА

## ✅ ЧТО УЖЕ СДЕЛАНО

Все файлы обновлены и готовы к работе:

1. ✅ `bot/keyboards/main_keyboards.py` - добавлены 3 новые клавиатуры
2. ✅ `bot/keyboards/__init__.py` - обновлены экспорты
3. ✅ `bot/handlers/uniquification.py` - добавлены 9 новых обработчиков
4. ✅ `main.py` - обновлен ConversationHandler с 8 новыми состояниями
5. ✅ `workers/tasks/uniquification_tasks.py` - расширена задача с 6 новыми параметрами
6. ✅ `workers/uniquification/photo_uniquifier.py` - добавлена логика обработки

## 📋 ПОСЛЕДОВАТЕЛЬНОСТЬ РАБОТЫ БОТА

### Шаг 1: Пользователь нажимает "🖼 Уникализировать фото"
### Шаг 2: Пользователь отправляет фото
### Шаг 3: Бот спрашивает количество копий (1-10)
### Шаг 4: 📁 Бот спрашивает "Выберите формат файла" → JPEG / PNG / WEBP
### Шаг 5: 🔄 Бот спрашивает "Отразить по горизонтали?" → Да / Нет / Назад в меню
### Шаг 6: ✍️ Бот спрашивает "Добавить текст?" → Да / Нет / Назад в меню
### Шаг 7: ➕ Бот спрашивает "Добавить дополнительное фото?" → Да / Нет / Назад в меню
   - Если Да → пользователь отправляет второе фото
   - Бот спрашивает позицию (Верх-лево/Верх-право/Низ-лево/Низ-право/Центр)
   - Бот спрашивает непрозрачность (0-100)
### Шаг 8: Бот создает копии и отправляет результат

## 🔧 КАК ЗАПУСТИТЬ

### Вариант 1: Локальный запуск

```bash
# 1. Остановите бота (если запущен)
# Нажмите Ctrl+C в терминале где запущен бот

# 2. Перейдите в директорию проекта
cd C:\Users\che_d\Desktop\уник\creo-bot

# 3. Запустите бота заново
python main.py
```

### Вариант 2: Если используется Celery

```bash
# Терминал 1 - Запустить Redis (если не запущен)
redis-server

# Терминал 2 - Запустить Celery Worker
cd C:\Users\che_d\Desktop\уник\creo-bot
celery -A workers.celery_app worker --loglevel=info

# Терминал 3 - Запустить бота
cd C:\Users\che_d\Desktop\уник\creo-bot
python main.py
```

### Вариант 3: Если бот на Railway/Heroku

```bash
# 1. Коммит изменений
cd C:\Users\che_d\Desktop\уник\creo-bot
git add .
git commit -m "Add advanced photo uniquification features"

# 2. Пуш на сервер
git push

# Бот автоматически перезапустится на сервере
```

## ✅ ПРОВЕРКА РАБОТЫ

После запуска:

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Нажмите "🖼 Уникализировать фото"
4. Отправьте любое фото
5. Выберите количество копий (например, 3)

**ТЕПЕРЬ ВЫ ДОЛЖНЫ УВИДЕТЬ:**

```
📁 Выберите формат файла:
[JPEG] [PNG] [WEBP]
[◀️ Назад в меню]
```

6. Выберите формат (например, JPEG)

**СЛЕДУЮЩИЙ ЭКРАН:**

```
🔄 Отразить по горизонтали?
[✅ Да] [❌ Нет]
[◀️ Назад в меню]
```

7. Выберите (например, Нет)

**СЛЕДУЮЩИЙ ЭКРАН:**

```
✍️ Добавить текст?
[✅ Да] [❌ Нет]
[◀️ Назад в меню]
```

8. И так далее...

## ❌ ЕСЛИ НЕ РАБОТАЕТ

### Проблема: После выбора количества сразу начинается обработка (старое поведение)

**Решение:**
```bash
# Убедитесь что бот ПОЛНОСТЬЮ остановлен
# Проверьте процессы Python
tasklist | findstr python

# Если есть процессы - завершите их
taskkill /F /IM python.exe

# Запустите бота заново
python main.py
```

### Проблема: Ошибка импорта

```bash
# Проверьте файлы
dir bot\handlers\uniquification.py
dir bot\keyboards\main_keyboards.py
dir main.py

# Если файлы есть - проверьте синтаксис
python -m py_compile bot\handlers\uniquification.py
python -m py_compile bot\keyboards\main_keyboards.py
python -m py_compile main.py
```

### Проблема: Бот не отвечает

```bash
# Проверьте логи
type logs\bot.log

# Проверьте что бот запущен
# В консоли должно быть:
# [MAIN] Bot is now running!
```

## 📝 ФАЙЛЫ С КОДОМ

Все обработчики находятся в:
- `bot/handlers/uniquification.py` - строки 91-310 (новые обработчики)
- `main.py` - строки 97-113 (регистрация обработчиков)
- `bot/keyboards/main_keyboards.py` - строки 114-155 (новые клавиатуры)

## 🎯 БЫСТРЫЙ ТЕСТ

Откройте файл и проверьте что функции есть:

```bash
# Проверка что новые функции импортируются
grep "select_copies_count" main.py
grep "select_file_format" main.py
grep "get_file_format_keyboard" bot/keyboards/__init__.py
```

Если команды вернули результат - код на месте!

## 💡 СОВЕТ

Если вы НЕ видите новые шаги после перезапуска:

1. Полностью закройте Telegram на телефоне
2. Откройте заново
3. Попробуйте функцию уникализации снова

Иногда Telegram кэширует старые состояния бота.

---

## 📞 ЧТО ДЕЛАТЬ ЕСЛИ НИЧЕГО НЕ ПОМОГЛО

Напишите какую ИМЕННО ошибку вы видите:
- Текст ошибки в консоли
- На каком шаге останавливается
- Что показывает бот в Telegram

Код 100% рабочий, просто нужен перезапуск!
