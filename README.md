# CreoMetrics Bot

Telegram-бот для обработки, уникализации и конвертации медиа-контента.

## Функции

- ✅ **Уникализация фото** - создание уникальных копий изображений с добавлением шумов, звездочек, бликов
- ✅ **Уникализация видео** - создание уникальных копий видео с различными эффектами
- ✅ **MP3 в голосовое сообщение** - конвертация аудио в формат Telegram voice
- ✅ **MP4 в видеосообщение** - конвертация видео в круглое видеосообщение
- ✅ **Сжатие видео** - автоматическое сжатие видео до нужного размера
- ✅ **Транскрибация видео** - распознавание речи и перевод на русский
- ✅ **Скачивание видео** - загрузка с YouTube и Instagram
- 🔜 **GPT чат** - интеграция с ChatGPT (в разработке)

## Технологии

- **Python 3.11**
- **python-telegram-bot** - Telegram Bot API
- **PostgreSQL** - база данных
- **Celery + Redis** - очередь задач
- **FFmpeg** - обработка видео/аудио
- **Pillow, OpenCV, MoviePy** - обработка изображений и видео
- **Whisper** - транскрибация (faster-whisper для оптимизации)
- **yt-dlp** - скачивание видео

## Установка

### 1. Клонирование репозитория

```bash
git clone <your-repo-url>
cd creo-bot
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Установка FFmpeg

#### Windows
Скачайте FFmpeg с [ffmpeg.org](https://ffmpeg.org/download.html) и добавьте в PATH.

#### Linux
```bash
sudo apt-get update
sudo apt-get install ffmpeg
```

#### macOS
```bash
brew install ffmpeg
```

### 4. Настройка окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните необходимые переменные:

```env
BOT_TOKEN=your_bot_token_here
DATABASE_URL=postgresql://user:password@localhost:5432/creo_bot
REDIS_URL=redis://localhost:6379/0
ADMIN_USER_IDS=123456789,987654321
REQUIRED_CHANNEL=@creometric
REQUIRED_CHANNEL_ID=-1001234567890
ADMIN_CHAT_ID=-1001234567890
```

### 5. Инициализация базы данных

```bash
python -c "from database import init_db; init_db()"
```

### 6. Добавление пресетов по умолчанию

```python
from database import SessionLocal
from database.models import UniquificationPreset, MediaType
from workers.uniquification.photo_uniquifier import create_default_photo_preset
from workers.uniquification.video_uniquifier import create_default_video_preset

db = SessionLocal()

# Фото пресет
photo_preset = UniquificationPreset(
    name="Default Photo",
    media_type=MediaType.PHOTO,
    is_default=True,
    config=create_default_photo_preset(),
    description="Пресет по умолчанию для фото"
)
db.add(photo_preset)

# Видео пресет
video_preset = UniquificationPreset(
    name="Default Video",
    media_type=MediaType.VIDEO,
    is_default=True,
    config=create_default_video_preset(),
    description="Пресет по умолчанию для видео"
)
db.add(video_preset)

db.commit()
db.close()
```

## Запуск

### Локальный запуск

#### 1. Запуск Redis
```bash
redis-server
```

#### 2. Запуск Celery Worker
```bash
celery -A workers.celery_app worker --loglevel=info --concurrency=2
```

#### 3. Запуск бота
```bash
python main.py
```

### Деплой на Railway

1. Создайте новый проект на [Railway](https://railway.app)
2. Добавьте PostgreSQL и Redis сервисы
3. Подключите GitHub репозиторий
4. Настройте переменные окружения
5. Railway автоматически задеплоит бот используя `Procfile`

## Структура проекта

```
creo-bot/
├── bot/                    # Telegram бот
│   ├── handlers/          # Обработчики команд и сообщений
│   ├── keyboards/         # Клавиатуры
│   ├── middlewares/       # Middleware (проверка подписки и др.)
│   └── filters/           # Фильтры
├── database/              # База данных
│   ├── models.py         # SQLAlchemy модели
│   └── database.py       # Подключение к БД
├── workers/               # Обработчики задач
│   ├── uniquification/   # Уникализация медиа
│   ├── conversion/       # Конвертация форматов
│   ├── compression/      # Сжатие видео
│   ├── transcription/    # Транскрибация
│   ├── download/         # Скачивание видео
│   ├── tasks/            # Celery задачи
│   └── celery_app.py     # Celery конфигурация
├── config/                # Конфигурация
│   └── settings.py       # Настройки приложения
├── assets/                # Ресурсы
│   └── placeholders/     # Изображения для меню
├── temp/                  # Временные файлы
├── logs/                  # Логи
├── main.py                # Точка входа бота
├── requirements.txt       # Зависимости
├── .env.example          # Пример переменных окружения
└── README.md             # Документация
```

## Система пресетов уникализации

Пресеты хранятся в БД и позволяют гибко настраивать уникализацию через админ-панель бота.

### Структура пресета для фото:

```json
{
  "methods": [
    {
      "name": "noise",
      "enabled": true,
      "intensity": [5, 15]
    },
    {
      "name": "sparkles",
      "enabled": true,
      "count": [10, 30],
      "size": [2, 5]
    },
    {
      "name": "lens_flare",
      "enabled": true,
      "intensity": [0.3, 0.7]
    }
  ]
}
```

### Доступные методы для фото:
- `noise` - добавление шума
- `sparkles` - добавление звездочек/бликов
- `lens_flare` - эффект блика от объектива
- `rotate` - небольшой поворот
- `brightness` - изменение яркости
- `contrast` - изменение контраста
- `hue` - изменение оттенка
- `blur` - небольшое размытие

### Доступные методы для видео:
- `speed` - изменение скорости
- `brightness` - изменение яркости
- `contrast` - изменение контраста
- `saturation` - изменение насыщенности
- `hue` - изменение оттенка
- `noise` - добавление шума
- `sparkles` - добавление звездочек
- `lens_flare` - блики

## Админ-панель

Доступна пользователям из `ADMIN_USER_IDS`.

Функции:
- Просмотр статистики
- Управление пользователями
- Настройка пресетов уникализации
- Рассылка сообщений
- Просмотр логов

## Логирование

Все действия пользователей логируются в:
1. База данных (таблица `action_logs`)
2. Админ-чат в Telegram (только оригинальные файлы)
3. Файлы логов (`logs/bot.log`)

## Миграция со старой версии

TODO: Добавить скрипт миграции после предоставления старой БД

## Лицензия

Proprietary - все права защищены

## Поддержка

По всем вопросам: @creometric
