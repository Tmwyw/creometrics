# 🚀 СТАРТ БОТА - Пошаговая Инструкция

## ✅ Что уже настроено:

- ✅ Токен бота: `8585315624:AAF0c6m6A5K2JkHUbh9zBMizUF4EZmsUFYc`
- ✅ Канал: `@creometric` (ID: -1002484700092)
- ✅ Админ: `@LazyEntrepreneur`
- ⏳ Нужен твой Telegram ID

## 📋 Быстрый Старт (Локально)

### Шаг 1: Получи свой Telegram ID

1. Открой [@userinfobot](https://t.me/userinfobot)
2. Нажми `/start`
3. Скопируй свой ID (число)

### Шаг 2: Добавь ID в .env

Открой файл `.env` и замени строку:

```env
ADMIN_USER_IDS=
```

на (подставь свой ID):

```env
ADMIN_USER_IDS=123456789
```

### Шаг 3: Установи зависимости

Нужны:
- Python 3.11
- PostgreSQL
- Redis
- FFmpeg

#### Windows:

```bash
# Python 3.11
# Скачай с https://www.python.org/downloads/

# PostgreSQL
# Скачай с https://www.postgresql.org/download/windows/

# Redis
# Скачай с https://github.com/microsoftarchive/redis/releases

# FFmpeg
# Скачай с https://ffmpeg.org/download.html
# Распакуй и добавь в PATH

# Установи Python зависимости
pip install -r requirements.txt
```

### Шаг 4: Настрой базу данных

```bash
# Создай БД
# Через psql:
psql -U postgres
CREATE DATABASE creo_bot;
\q

# Обнови .env если нужно:
DATABASE_URL=postgresql://postgres:твой_пароль@localhost:5432/creo_bot
```

### Шаг 5: Инициализация

```bash
# Создать таблицы
python -c "from database import init_db; init_db()"

# Создать пресеты
python scripts/init_presets.py

# Создать админа (после получения ID из шага 1)
python scripts/create_admin.py ТвойTelegramID LazyEntrepreneur
```

### Шаг 6: Запуск (3 терминала)

**Терминал 1 - Redis:**
```bash
redis-server
```

**Терминал 2 - Celery Worker:**
```bash
celery -A workers.celery_app worker --loglevel=info --concurrency=2
```

**Терминал 3 - Bot:**
```bash
python main.py
```

### Шаг 7: Тест

1. Открой бота в Telegram
2. Отправь `/start`
3. Подпишись на @creometric если попросит
4. Увидишь главное меню

---

## 🚀 Быстрый Деплой на Railway (Рекомендуется)

Railway автоматически установит всё нужное.

### Шаг 1: Подготовь код

```bash
# Инициализируй git (если еще не сделано)
cd creo-bot
git init
git add .
git commit -m "Initial commit"

# Создай репозиторий на GitHub
# Запуши код
git remote add origin https://github.com/твой_username/creo-bot.git
git push -u origin main
```

### Шаг 2: Railway Setup

1. Открой [Railway.app](https://railway.app)
2. Войди через GitHub
3. New Project → Deploy from GitHub repo
4. Выбери репозиторий `creo-bot`

### Шаг 3: Добавь сервисы

В Railway проекте:
- Add Service → PostgreSQL
- Add Service → Redis

### Шаг 4: Настрой переменные

Railway → твой проект → Variables

Добавь:
```env
BOT_TOKEN=8585315624:AAF0c6m6A5K2JkHUbh9zBMizUF4EZmsUFYc
ADMIN_USER_IDS=ТвойTelegramID
REQUIRED_CHANNEL=@creometric
REQUIRED_CHANNEL_ID=-1002484700092
ADMIN_CHAT_ID=-1002484700092
ENVIRONMENT=production
DEBUG=False
```

Railway автоматически добавит:
- `DATABASE_URL` (из PostgreSQL)
- `REDIS_URL` (из Redis)

### Шаг 5: Деплой

Railway автоматически задеплоит после настройки.

### Шаг 6: Инициализация на Railway

Railway → твой проект → Settings → Railway CLI

Или через Web Terminal:

```bash
python scripts/init_presets.py
python scripts/create_admin.py ТвойTelegramID LazyEntrepreneur
```

### Шаг 7: Готово!

Бот работает 24/7 на Railway.

---

## ❓ Что Сейчас Работает

- ✅ `/start` - регистрация и главное меню
- ✅ Проверка подписки на канал
- ✅ Главное меню с кнопками
- ⏳ Обработчики функций (нужно дописать)

## 🔨 Что Нужно Доделать

См. файл `TODO.md` - детальный список.

**Главное:**
1. Дописать handlers для всех функций
2. Реализовать отправку результатов
3. Добавить админ-панель

**Оценка:** 15-20 часов работы

---

## 📞 Помощь

Если что-то не работает:

1. Проверь логи: `tail -f logs/bot.log`
2. Проверь `.env` файл
3. Убедись что Redis и PostgreSQL запущены
4. Проверь что FFmpeg установлен: `ffmpeg -version`

## 🎯 Следующие Шаги

После успешного запуска:

1. Протестируй `/start`
2. Прочитай `TODO.md`
3. Начни дописывать handlers (примеры уже есть)
4. Или дай знать если нужна помощь!

---

**Удачи! Бот готов к запуску! 🚀**
