# Обновление функции уникализации фото

## Что было добавлено

Расширена функциональность уникализации фото с добавлением новых опций:

### 1. Выбор формата файла
- **JPEG** - стандартный формат с высоким качеством
- **PNG** - формат без потерь качества
- **WEBP** - современный формат с хорошим сжатием

### 2. Отражение по горизонтали
- Возможность зеркально отразить изображение
- Выбор: Да / Нет

### 3. Добавление текста
- Наложение текста на изображение
- Текст размещается внизу по центру с тенью для лучшей читаемости
- Размер шрифта автоматически подбирается под размер изображения

### 4. Добавление дополнительного фото
- Наложение второго изображения поверх основного
- **Позиции**:
  - Верх-лево
  - Верх-право
  - Низ-лево
  - Низ-право
  - Центр
- **Непрозрачность**: от 0 (полностью прозрачное) до 100 (полностью непрозрачное)
- Размер накладываемого изображения автоматически уменьшается до 30% от основного

## Новый процесс уникализации

1. Пользователь нажимает "🖼 Уникализировать фото"
2. Отправляет фото
3. Выбирает количество копий (1-10)
4. **Новое:** Выбирает формат файла (JPEG / PNG / WEBP)
5. **Новое:** Выбирает, отразить ли по горизонтали (Да / Нет)
6. **Новое:** Выбирает, добавить ли текст (Да / Нет)
   - Если Да → вводит текст
7. **Новое:** Выбирает, добавить ли дополнительное фото (Да / Нет)
   - Если Да:
     - Отправляет второе фото
     - Выбирает позицию (Верх-лево/Верх-право/Низ-лево/Низ-право/Центр)
     - Вводит непрозрачность (0-100)
8. Бот создает копии с учетом всех параметров и отправляет результат

## Измененные файлы

### 1. `bot/keyboards/main_keyboards.py`
- Добавлена `get_file_format_keyboard()` - выбор формата
- Добавлена `get_yes_no_keyboard()` - универсальная клавиатура Да/Нет
- Добавлена `get_overlay_position_keyboard()` - выбор позиции наложения

### 2. `bot/handlers/uniquification.py`
- Добавлены новые состояния разговора:
  - `WAITING_FOR_FILE_FORMAT`
  - `WAITING_FOR_FLIP_CHOICE`
  - `WAITING_FOR_TEXT_CHOICE`
  - `WAITING_FOR_TEXT_INPUT`
  - `WAITING_FOR_OVERLAY_CHOICE`
  - `WAITING_FOR_OVERLAY_PHOTO`
  - `WAITING_FOR_OVERLAY_POSITION`
  - `WAITING_FOR_OVERLAY_OPACITY`

- Добавлены новые обработчики:
  - `select_copies_count()` - выбор количества копий
  - `select_file_format()` - выбор формата файла
  - `select_flip_choice()` - выбор отражения
  - `select_text_choice()` - выбор добавления текста
  - `receive_text_input()` - получение текста
  - `select_overlay_choice()` - выбор добавления фото
  - `receive_overlay_photo()` - получение второго фото
  - `select_overlay_position()` - выбор позиции
  - `receive_overlay_opacity()` - получение непрозрачности

- Обновлен `process_photo_uniquification()`:
  - Загрузка дополнительного фото при необходимости
  - Передача всех новых параметров в Celery задачу

### 3. `main.py`
- Обновлены импорты из `bot.handlers.uniquification`
- Расширен `ConversationHandler` для фото с новыми состояниями

### 4. `workers/tasks/uniquification_tasks.py`
- Обновлена сигнатура `uniquify_photo_task()`:
  - Добавлен `file_format` (по умолчанию 'jpeg')
  - Добавлен `flip_horizontal` (по умолчанию False)
  - Добавлен `overlay_text` (опционально)
  - Добавлен `overlay_photo_path` (опционально)
  - Добавлен `overlay_position` (опционально)
  - Добавлен `overlay_opacity` (опционально)
- Параметры добавляются в конфигурацию перед созданием `PhotoUniquifier`

### 5. `workers/uniquification/photo_uniquifier.py`
- Обновлен `__init__()`:
  - Добавлено сохранение новых параметров из конфигурации

- Обновлен `uniquify()`:
  - Применение горизонтального отражения через `ImageOps.mirror()`
  - Применение текстового наложения через `_apply_text_overlay()`
  - Применение фото наложения через `_apply_photo_overlay()`
  - Сохранение в выбранном формате (JPEG/PNG/WEBP)

- Добавлены новые методы:
  - `_apply_text_overlay()` - наложение текста с тенью
  - `_apply_photo_overlay()` - наложение второго фото с настройкой позиции и прозрачности

## Технические детали

### Форматы файлов
- **JPEG**: качество 95, быстрое сжатие
- **PNG**: без потерь, поддержка прозрачности
- **WEBP**: качество 95, современный формат

### Наложение текста
- Размер шрифта: 5% от высоты изображения (минимум 20px)
- Позиция: внизу по центру с отступом 20px
- Тень: черная с непрозрачностью 200, смещение 2px
- Текст: белый с непрозрачностью 255

### Наложение фото
- Размер: 30% от ширины основного изображения
- Пропорции сохраняются
- Отступы от краев: 20px
- Непрозрачность регулируется через альфа-канал

## Совместимость

Все изменения обратно совместимы:
- Старые пресеты продолжают работать
- Новые параметры опциональны с разумными значениями по умолчанию
- При отсутствии новых параметров поведение идентично предыдущей версии

## Тестирование

Рекомендуется протестировать:
1. ✅ Базовую уникализацию без новых опций
2. ✅ Каждый формат файла отдельно
3. ✅ Отражение по горизонтали
4. ✅ Добавление текста (короткий и длинный)
5. ✅ Добавление фото в разных позициях
6. ✅ Различные значения непрозрачности (0, 50, 100)
7. ✅ Комбинацию всех опций одновременно
